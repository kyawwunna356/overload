# Copy to .env.local and fill in from Supabase: Project Settings → API.
# Both are public by design (the anon key only works through RLS). Leave them unset and the
# app runs local-only, with nothing syncing.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
